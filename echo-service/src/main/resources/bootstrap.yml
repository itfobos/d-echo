server:
  port: 8778

spring:
  application:
    name: echo-service
  cloud:
    config:
      uri: http://config:8888
      username: ${CONFIG_SERVICE_USER}
      password: ${CONFIG_SERVICE_PASSWORD}
      fail-fast: true